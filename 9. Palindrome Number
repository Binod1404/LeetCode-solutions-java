class Solution {
    public boolean isPalindrome(int x) {
        int res = 0;
        int val = x;
        while(val>0) {
            int num = val %10;
            res = res*10 + num;
            val = val/10;
        }
        return x == res;

        
    }
}
